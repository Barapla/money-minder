<%= render Calendar::MainComponent.new(title: 'Calendario de transacciones', date:, transactions:, payroll_reminders: payroll_reminders_by_date || {}, options: { params: { filter: params[:filter] } }) %>