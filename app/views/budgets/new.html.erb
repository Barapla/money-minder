<div class="min-h-screen p-4 sm:p-6 lg:p-8">
    <div class="max-w-4xl mx-auto">
        <!-- Header -->
        <div class="bg-gradient-to-r from-bunker-900/80 to-purple-900/80 backdrop-blur-lg border border-bunker-800/50 rounded-2xl p-8 mb-8">
            <div class="flex items-center gap-4 mb-4">
                <%= render Buttons::LinkComponent.new(content: '
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                    </svg>
                ', href: budgets_path, options: { class: "btn-icon" }) %>
                <div>
                    <h1 class="text-4xl font-bold text-white">
                        <span class="text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-purple-300">💰 Nuevo Presupuesto</span>
                    </h1>
                    <p class="text-bunker-300">Crea un nuevo presupuesto para controlar tus gastos</p>
                </div>
            </div>
        </div>

        <!-- Form Container -->
        <div class="bg-bunker-900/50 backdrop-blur-lg border border-bunker-800/50 rounded-2xl overflow-hidden">
            <!-- Form Header -->
            <div class="px-8 py-6 border-b border-bunker-800/50">
                <h2 class="text-xl font-semibold text-white flex items-center gap-2">
                    <svg class="w-6 h-6 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                    </svg>
                    Información del Presupuesto
                </h2>
            </div>

            <!-- Form -->
            <%= form_with(model: @budget, local: true, html: { class: "px-8 py-6", 
                data: { controller: "budgets--preview form-validator", 
                    form_validator_validate_on_blur_value: true,
                    form_validator_validate_on_input_value: false} }) do |form| %>
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                    <!-- Left Column -->
                    <div class="space-y-6">
                        
                        <%= render Forms::InputComponent.new(
                            name: :name,
                            form:,
                            type: :text,
                            options: { 
                                placeholder: "Nuevo Presupuesto",
                                help_text: "Nombre descriptivo del presupuesto",
                                required: true,
                                data: {
                                    "budgets--preview-target": "name",
                                    action: "input->budgets--preview#updatePreview",
                                    required_message: "Por favor, ingresa un nombre para el presupuesto",
                                }
                            }
                        ) %>
                        
                        <!-- Budget Type -->
                        <%= render Forms::SelectComponent.new(
                            name: :budget_type_id,
                            form:,
                            options_collection: Catalog.get_by_group_pluck('budget_types'),
                            options: { 
                                help_text: "Categoría principal del presupuesto",
                                required: true,
                                data: {
                                    "budgets--preview-target": "budgetType",
                                    action: "change->budgets--preview#updatePreview"
                                }
                            }
                        ) %>

                        <%= render Forms::InputComponent.new(
                            name: :current_amount,
                            form:,
                            type: :number,
                            options: { 
                                step: 0.01,
                                placeholder: "0.00",
                                prefix: "$",
                                help_text: "Cantidad ya gastada en este período",
                                required: true,
                                data: {
                                    "budgets--preview-target": "current",
                                    action: "input->budgets--preview#updatePreview",
                                    required_message: "Por favor, ingresa la cantidad ya gastada"
                                }
                            }
                        ) %>

                        <%= render Forms::InputComponent.new(
                            name: :limit_amount,
                            form:,
                            type: :number,
                            options: { 
                                step: 0.01,
                                placeholder: "0.00",
                                prefix: "$",
                                help_text: "Cantidad máxima que planeas gastar",
                                required: true,
                                data: {
                                    "budgets--preview-target": "limit",
                                    action: "input->budgets--preview#updatePreview",
                                    required_message: "Por favor, ingresa el límite del presupuesto"
                                }
                            }
                        ) %>
                    </div>

                    <!-- Right Column -->
                    <div class="space-y-6">
                        <!-- Icon Selection -->
                        <%= render Forms::RadioComponent.new(
                            name: :icon_id,
                            form:,
                            collection: Catalog.by_group('budget_icons'),
                            options: { 
                                default_object_label: 'value',
                                help_text: "Selecciona un icono para identificar tu presupuesto",
                                collection_options: {
                                    data: {
                                        "budgets--preview-target": "icons",
                                        action: "click->budgets--preview#selectIcon"
                                    }
                                }
                            }
                        ) %>

                        <!-- Color Selection -->
                        <%= render Forms::RadioComponent.new(
                            name: :color_id,
                            form:,
                            collection: Catalog.by_group('budget_colors'),
                            options: { 
                                is_colors: true,
                                default_object_label: 'value',
                                help_text: "Elige un color para personalizar tu presupuesto",
                                collection_options: {
                                    data: {
                                        "budgets--preview-target": "colors",
                                        action: "click->budgets--preview#selectColor"
                                    }
                                }
                            }
                        ) %>

                        <!-- Preview Card -->
                        <div class="form-group">
                            <label class="block text-sm font-medium text-bunker-200 mb-2">
                                Vista Previa
                            </label>
                            <div class="bg-bunker-800/60 border border-bunker-700/50 rounded-xl p-4">
                                <div class="flex items-center gap-3 mb-3">
                                    <div data-budgets--preview-target="preview" class="w-10 h-10 bg-blue-500 rounded-lg flex items-center justify-center">
                                        <span class="text-lg">💵</span>
                                    </div>
                                    <div>
                                        <div data-budgets--preview-target="previewBudgetType" class="text-sm font-medium text-white">Efectivo</div>
                                        <div data-budgets--preview-target="previewName" class="text-xs text-bunker-400">Nuevo Presupuesto</div>
                                    </div>
                                </div>
                                <div class="space-y-2 text-xs">
                                    <div class="flex justify-between">
                                        <span class="text-bunker-300">Gastado</span>
                                        <span data-budgets--preview-target="previewCurrent" class="text-orange-400">$0.00</span>
                                    </div>
                                    <div class="flex justify-between">
                                        <span class="text-bunker-300">Límite</span>
                                        <span data-budgets--preview-target="previewLimit" class="text-white">$0.00</span>
                                    </div>
                                    <div class="bg-bunker-700 rounded-full h-2">
                                        <div data-budgets--preview-target="previewProgressBar" class="bg-purple-400 h-2 rounded-full" style="width: 0%"></div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Form Actions -->
                <div class="flex items-center justify-between pt-8 mt-8 border-t border-bunker-800/50">
                    <%= render Buttons::LinkComponent.new(content: '
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                        </svg>
                        Cancelar
                        ', href: budgets_path, 
                        options: { 
                            class: "btn-cancel" 
                        })
                    %>
                    <div class="flex gap-3">
                        <button type="button" class="px-6 py-3 text-white border border-purple-500/50 hover:bg-purple-500/10 rounded-xl font-medium transition-all duration-200 flex items-center gap-2">
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                            </svg>
                            Vista Previa
                        </button>
                        
                        <button type="submit" class="bg-gradient-to-r from-purple-500 to-purple-600 hover:from-purple-600 hover:to-purple-700 text-white px-8 py-3 rounded-xl font-semibold transition-all duration-200 hover:-translate-y-0.5 hover:shadow-lg hover:shadow-purple-500/30 flex items-center gap-2">
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                            </svg>
                            Crear Presupuesto
                        </button>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</div>