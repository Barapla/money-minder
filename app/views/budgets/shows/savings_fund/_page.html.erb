<div class="max-w-7xl mx-auto">
  <%= render 'budgets/shows/savings_fund/hero' %>
  <%= render 'budgets/shows/savings_fund/stats' %>

  <div class="grid grid-cols-1 xl:grid-cols-[minmax(0,1.6fr)_minmax(0,1fr)] gap-5">
    <div class="flex flex-col gap-5">
      <%= render 'budgets/shows/savings_fund/growth' %>
      <%= render 'budgets/shows/savings_fund/movements' %>
    </div>
    <div class="flex flex-col gap-5">
      <%= render 'budgets/shows/savings_fund/sidebar' %>
    </div>
  </div>
</div>
