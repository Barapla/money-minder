<%= turbo_frame_tag "modal_frame" do %>
    <%= render Shared::ModalComponent.new(title: "Nuevas transacciones recurrentes", subtitle: "Configura una transacción que se repita automáticamente") do |component| %>
        <%= render "recurring_transactions/modals/form", transaction: @transaction %>
    <% end %>
<% end %>